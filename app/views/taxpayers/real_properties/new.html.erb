<%= simple_form_for [@taxpayer, @real_property] do |f| %>
  <%= f.input :description %>
  <%= f.input :type, collection: RealProperty.types.map{|a| [a, a] } %>
  <%= f.submit "Save Real Property" %>
<% end %>
